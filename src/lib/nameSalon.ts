export const obtenerNombreSalon = (salon: string) => {
    if (salon === '0') return 'Todos';
    if (salon === 'A') return 'Rut Haus / Origami';
    if (salon === 'B') return 'El Central / La Rural';
};
